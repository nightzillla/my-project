import React from 'react'

function Projects() {
  return (
    <div>Projects
      trying to see if message will work on windows,
      using react on windows pc. so fare git commands are not working.
      just updated vs code with github cli. checking to see if git push will work
    </div>
  )
}

export default Projects